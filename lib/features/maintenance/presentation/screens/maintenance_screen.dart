import '../../../../shared/presentation/widgets/common/feature_placeholder_screen.dart';

class MaintenanceScreen extends FeaturePlaceholderScreen {
  const MaintenanceScreen({super.key})
    : super(
        title: 'Maintenance',
        description:
            'Maintenance remains scaffold-only while the core foundations are being finalized.',
      );
}
