import '../../../../shared/presentation/widgets/common/feature_placeholder_screen.dart';

class ProfileScreen extends FeaturePlaceholderScreen {
  const ProfileScreen({super.key})
    : super(
        title: 'Profile',
        description: 'Deferred until auth and session work are ready.',
      );
}
