import '../../../../shared/presentation/widgets/common/feature_placeholder_screen.dart';

class TasksListScreen extends FeaturePlaceholderScreen {
  const TasksListScreen({super.key})
    : super(
        title: 'Tasks',
        description:
            'Tasks are intentionally left as placeholders until their phase work begins.',
      );
}
